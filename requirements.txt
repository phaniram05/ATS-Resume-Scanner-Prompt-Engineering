streamlit
google-generativeai
python-dotenv
pkgconfig
cmake
poppler-utils
python-poppler
pdf2image


# Poppler has also been installed as a dependency for pdf2image

# From the terminal, use the command
# brew install poppler